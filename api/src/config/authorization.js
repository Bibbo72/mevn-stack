/**
 * AUTHORIZATION CONFIGURATION
 *
 * CURRENTLY UNDER CONSTRUCTION
 *
 * The plan is to use this configuration for Access Control.
 * It should export an instance of the configuration as middleware
 * for HTTP requests.
 */

// import acl from 'acl'
// import client from './cache'

// let node_acl = new acl(new acl.redisBackend(client, "acl_"))

// node_acl.allow("Guest", "", "view")

// export default node_acl